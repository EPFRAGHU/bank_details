[pytest]
# Only collect the real suite. The repo root holds ad-hoc scratch scripts
# named test_*.py / *_test.py that would otherwise be collected and can hang
# on live-server calls.
testpaths = tests
